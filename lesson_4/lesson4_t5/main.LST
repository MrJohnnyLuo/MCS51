C51 COMPILER V9.02   MAIN                                                                  03/07/2020 14:42:06 PAGE 1   


C51 COMPILER V9.02, COMPILATION OF MODULE MAIN
OBJECT MODULE PLACED IN main.OBJ
COMPILER INVOKED BY: D:\Program Files (x86)\Keil\C51\BIN\C51.EXE main.c BROWSE DEBUG OBJECTEXTEND

line level    source

   1          #include<reg52.h>
   2          
   3          sbit ADDR0 = P1^0;
   4          sbit ADDR1 = P1^1;
   5          sbit ADDR2 = P1^2;
   6          sbit ADDR3 = P1^3;
   7          sbit ENLED = P1^4;
   8          
   9          void main()
  10          {
  11   1              unsigned int i = 0;             //定义循环变量i，用于软件延时
  12   1              unsigned char dir = 0;          //定义移位方向变量dir，用于控制移位的方向
  13   1              unsigned char shift = 0x01;             //定义循环移位变量shift，并赋初值0x01
  14   1      
  15   1              ENLED = 0;
  16   1              ADDR3 = 1;
  17   1              ADDR2 = 1;
  18   1              ADDR1 = 1;
  19   1              ADDR0 = 0;
  20   1      
  21   1              while(1)                //主循环，程序无限循环执行该循环体语句
  22   1              {
  23   2                      P0 = ~shift;                    //P0等于循环移位变量取反，控制8个LED
  24   2                      for(i=0; i<30000; i++); //软件延时
  25   2                      if(dir == 0)                    //移位方向变量为0时，左移
  26   2                      {
  27   3                              shift = shift << 1;     //循环移位变量左移1位
  28   3                              if(shift == 0x80)       //左移到最左端后，改变移位方向
  29   3                              {
  30   4                                      dir = 1;
  31   4                              }
  32   3                      }
  33   2                      else                                    //移位方向变量不为0时，右移
  34   2                      {
  35   3                              shift = shift >> 1;     //循环移位变量右移1位
  36   3                              if(shift == 0x01)       //右移到最右端后，改变移位方向
  37   3                              {
  38   4                                      dir = 0;
  39   4                              }
  40   3                      }
  41   2              }
  42   1      }


MODULE INFORMATION:   STATIC OVERLAYABLE
   CODE SIZE        =     57    ----
   CONSTANT SIZE    =   ----    ----
   XDATA SIZE       =   ----    ----
   PDATA SIZE       =   ----    ----
   DATA SIZE        =   ----    ----
   IDATA SIZE       =   ----    ----
   BIT SIZE         =   ----    ----
END OF MODULE INFORMATION.


C51 COMPILATION COMPLETE.  0 WARNING(S),  0 ERROR(S)
